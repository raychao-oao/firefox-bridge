{
  "name": "firefox-bridge",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "native-host",
    "mcp-server",
    "firefox-bridge-bot"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "npm run test -w native-host && npm run test -w mcp-server",
    "sign": "node scripts/sign-extension.js"
  }
}
